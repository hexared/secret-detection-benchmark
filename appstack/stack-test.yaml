hasura:
  image: hasura/graphql-engine:v2.1.1
  command:
  - graphql-engine
  - serve
  depends_on:
      cots-db:
          condition: service_started
  environment:
      HASURA_GRAPHQL_DATABASE_URL: postgresql://mainuser:bestpw2026@maindb:5432/data
      HASURA_GRAPHQL_ENABLE_CONSOLE: "true"
      HASURA_GRAPHQL_LOG_LEVEL: warn
      HASURA_GRAPHQL_QUERY_PLAN_CACHE_SIZE: "100"
      HASURA_GRAPHQL_SERVER_PORT: "3000"
      HASURA_GRAPHQL_V1_BOOLEAN_NULL_COLLAPSE: "true"
hasura2:
  image: hasura/graphql-engine:v2.1.1
  command:
  - graphql-engine
  - serve
  depends_on:
      cots-db:
          condition: service_started
  environment:
      HASURA_GRAPHQL_DATABASE_URL: postgresql://adminuser:sUp3rb3stpw2026@maindb:5432/data
      HASURA_GRAPHQL_ENABLE_CONSOLE: "true"
      HASURA_GRAPHQL_LOG_LEVEL: warn
      HASURA_GRAPHQL_QUERY_PLAN_CACHE_SIZE: "100"
      HASURA_GRAPHQL_SERVER_PORT: "3000"
      HASURA_GRAPHQL_V1_BOOLEAN_NULL_COLLAPSE: "true"

# just a test comment to check password hiding
# connect with PASSWORD: omgAnother$ecret123
